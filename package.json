{
  "name": "3d-engine",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c",
    "start": "npm run server; npm run watch;",
    "server": "live-server --open='build/' --watch='build/'",
    "watch": "watch \"rollup -c\" src/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015-rollup": "^1.1.1",
    "live-server": "^0.9.2",
    "rollup": "^0.25.8",
    "rollup-plugin-babel": "^2.4.0",
    "watch": "^0.17.1"
  }
}
